Drop generated PNGs here using the exact filenames below (lowercase, no spaces).
The app looks for `/spark-images/kids/<name>.png` — if a file is missing it falls
back to a plain "Image needed" placeholder, so the lesson still works while you're
generating images.

See the Midjourney prompt list provided in chat for what each one should show.
